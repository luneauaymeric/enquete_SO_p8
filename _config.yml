# Book settings
# Learn more at https://jupyterbook.org/customize/config.html

# Book settings
title                       : Enquête sur les besoins à 8 en matière de Science Ouverte
author                      : Aymeric Luneau, BU Paris 8  # The author of the book
copyright                   : "2022"  # Copyright year to be placed in the footer
logo                        : logo.png



#logo: 

# Force re-execution of notebooks on each build.
# See https://jupyterbook.org/content/execute.html
execute:
  execute_notebooks: 'off'

# Define the name of the latex output file for PDF builds
latex:
  latex_engine              : pdflatex  # one of 'pdflatex', 'xelatex' (recommended for unicode), 'luatex', 'platex', 'uplatex'
  use_jupyterbook_latex     : true # use sphinx-jupyterbook-latex for pdf builds as default
  latex_documents:
    targetname: enquete_so_p8.tex

# Add a bibtex file so that we can create citations
bibtex_bibfiles:
  - Open_data_biblio.bib

# Information about where the book exists on the web
repository:
  url: https://github.com/luneauaymeric/enquete_SO_p8 # Online location of your book
  #path_to_book: docs  # Optional path to your book, relative to the repository root
  branch: main  # Which branch of the repository should be used when creating links (optional)

# Add GitHub buttons to your book
# See https://jupyterbook.org/customize/config.html#add-a-link-to-your-repository
html:
  use_issues_button: false
  use_repository_button: false
  comments:
    hypothesis: true

sphinx:
  config:
    bibtex_reference_style: author_year